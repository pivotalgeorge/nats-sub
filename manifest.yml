---
applications:

- name: nats-sub
  buildpack: ruby_buildpack
  env:
    NATS_USER: nats
    NATS_PASS: <PASSWORD>
    ROLE: subscribe

- name: nats-pub
  buildpack: ruby_buildpack
  env:
    NATS_USER: nats
    NATS_PASS: <PASSWORD>
    ROLE: publish

